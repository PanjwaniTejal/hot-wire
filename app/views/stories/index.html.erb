<%= link_to 'Create new story', new_story_path, class: 'btn btn-dark' %>
<h2 class='mt-3'>Stories</h2>

<% if @stories.any? %>
  <%= render partial: 'story', collection: @stories %>
<% else %>
  <p>No stories available</p>
<% end %>
